{{#each clientes}}
    <div class="card mt-4">

        <div class="card text-center">
        <div class="card-header">
            Featured
        </div>
        <div class="card-body">
            <h2 class="card-title">{{dataValues.name}} {{dataValues.lastName}}</h2>
            <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
            <a href="#" class="btn btn-primary">Atualizar dados do cliente</a>
        </div>
        <div class="card-footer text-muted">
            Ultima edição?
        </div>
        </div>

    </div>
{{/each}}

<a href="http://localhost:8081/receptionist/index">
        <button class="btn btn-success" style="background-color:#0B090A ; border-color:#0B090A;">voltar para pagina inicial</button>
    </a>